export interface proveedorBaseData {
    PROVEEDOR_RAZONSOCIAL: string;
    PROVEEDOR_RUT: string;
    PROVEEDOR_GIRO: string;
    PROVEEDOR_DIRECCION: string;
    PROVEEDOR_TELEFONO: string;
    PROVEEDOR_EMAIL: string;
    PROVEEDOR_CIUDAD: string;
}